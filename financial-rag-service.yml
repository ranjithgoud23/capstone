apiVersion: v1
kind: Service
metadata:
  name: financial-rag-service
spec:
  selector:
    app: financial-rag
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8501
